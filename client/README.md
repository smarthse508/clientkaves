# Cara menjalankan project ini di lokal

1. Setelah kamu clone, cek apakah package.json sudah ada atau belum. Jika belum => npm install
2. Jika sudah ada, jalankan project dengan perintah npm run dev